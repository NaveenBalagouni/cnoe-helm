apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "cnoe-helm.fullname" . }}-test-connection"
  labels:
    {{- include "cnoe-helm.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "cnoe-helm.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
