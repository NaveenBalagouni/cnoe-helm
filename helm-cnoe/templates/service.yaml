apiVersion: v1
kind: Service
metadata:
  name: {{ include "helm-cnoe.fullname" . }}
  labels:
    {{- include "helm-cnoe.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "helm-cnoe.selectorLabels" . | nindent 4 }}
